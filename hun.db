-- Run these commands on your laptop's MySQL server
CREATE USER 'fitai_user'@'%' IDENTIFIED BY 'secure_password';
GRANT ALL PRIVILEGES ON fitai.* TO 'fitai_user'@'%';
FLUSH PRIVILEGES;

-- Also make sure to update your MySQL bind address
-- Edit my.ini or my.cnf and change:
-- bind-address = 0.0.0.0  (instead of 127.0.0.1)